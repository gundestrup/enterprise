<%= tabnav :kitchen %>
<p>
  <b>Name:</b>
  <%=h @list.purchase.name %>
</p>
<table>

    <tr>

    </tr>
    
</table>
<% if logged_in? %>
    <%= link_to 'Edit', edit_list_path(@list) %> |
    <%= link_to 'New', new_list_path %> |
<% end %>
<%= link_to 'Back', lists_path %>

<br/>